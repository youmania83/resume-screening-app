[Unit]
Description=Run resume-screening-app database backup every 4 hours

[Timer]
# Every 4 hours, plus once ~2 min after boot in case the server was down
# across a scheduled run.
OnCalendar=*-*-* 00/4:00:00
OnBootSec=2min
Persistent=true
RandomizedDelaySec=60

[Install]
WantedBy=timers.target
